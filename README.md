# poc-test

PoC: can a file added by a **squash-merged** PR (whose branch was auto-deleted)
be removed from the PR view by later git operations (force-push main / restore
+ rewrite the branch)?

Mirrors stablelabs/stable PR #342 (added audit PDF) / #418 (replaced it).